<UserControl
    x:Class="BankDds.Wpf.Views.ReportsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:BankDds.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    Background="White"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Style="{StaticResource PageTitleStyle}"
            Text="Báo cáo và Thống kê" />

        <Border
            Grid.Row="1"
            Margin="0,0,0,10"
            Padding="10"
            Background="#D1ECF1"
            BorderBrush="#17A2B8"
            BorderThickness="1"
            CornerRadius="5"
            Visibility="{Binding HasError, Converter={StaticResource BoolToVisConverter}}">
            <TextBlock
                FontWeight="SemiBold"
                Foreground="#0C5460"
                Text="{Binding ErrorMessage}"
                TextWrapping="Wrap" />
        </Border>

        <TabControl Grid.Row="2">
            <TabItem Header="Sao kê tài khoản">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border
                        Grid.Row="0"
                        Padding="15"
                        Background="#F8F9FA"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="280" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Số tài khoản:" />
                                <ComboBox
                                    Height="42"
                                    IsEditable="True"
                                    ItemsSource="{Binding StatementAccounts}"
                                    Text="{Binding StatementAccountNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Style="{StaticResource StandardComboBoxStyle}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <Run Text="{Binding SOTK}" />
                                                <Run Text=" - " />
                                                <Run Text="{Binding BranchDisplayName}" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Từ ngày:" />
                                <DatePicker
                                    x:Name="StatementFromDate"
                                    MinHeight="32"
                                    Padding="10"
                                    FontSize="14" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Đến ngày:" />
                                <DatePicker
                                    x:Name="StatementToDate"
                                    MinHeight="32"
                                    Padding="10"
                                    FontSize="14" />
                            </StackPanel>

                            <Button
                                x:Name="GenerateStatement"
                                Grid.Column="3"
                                Margin="0,22,10,0"
                                Height="42"
                                Content="Tạo báo cáo"
                                Style="{StaticResource PrimaryButtonStyle}" />

                            <StackPanel Grid.Column="4" Margin="0,12,0,0">
                                <TextBlock Margin="0,0,0,2" FontSize="10" Text="Xuất:" />
                                <StackPanel Orientation="Horizontal">
                                    <Button
                                        x:Name="ExportStatementToPdf"
                                        Width="70"
                                        Height="42"
                                        Margin="0,0,5,0"
                                        Content="PDF"
                                        Style="{StaticResource SuccessButtonStyle}" />
                                    <Button
                                        x:Name="ExportStatementToExcel"
                                        Width="80"
                                        Height="42"
                                        Content="Excel"
                                        Style="{StaticResource SuccessButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border
                        Grid.Row="2"
                        Padding="15"
                        Background="White"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5"
                        Visibility="{Binding HasStatement, Converter={StaticResource BoolToVisConverter}}">
                        <StackPanel>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontWeight="Bold" Text="Số tài khoản:" />
                                    <TextBlock
                                        FontSize="18"
                                        Foreground="#0078D7"
                                        Text="{Binding AccountStatement.SOTK}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontWeight="Bold" Text="Số dư đầu:" />
                                    <TextBlock
                                        FontSize="18"
                                        Foreground="#28A745"
                                        Text="{Binding AccountStatement.OpeningBalance, StringFormat=N0}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontWeight="Bold" Text="Số dư cuối:" />
                                    <TextBlock
                                        FontSize="18"
                                        Foreground="#17A2B8"
                                        Text="{Binding AccountStatement.ClosingBalance, StringFormat=N0}" />
                                </StackPanel>
                            </Grid>

                            <Separator Margin="0,0,0,15" />
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Giao dịch" />

                            <DataGrid
                                Height="300"
                                ItemsSource="{Binding AccountStatement.Lines}"
                                Style="{StaticResource StandardDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Width="2*" Binding="{Binding OpeningBalance, StringFormat=N0}" Header="Số dư đầu" />
                                    <DataGridTextColumn Width="*" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Header="Ngày" />
                                    <DataGridTextColumn Width="*" Binding="{Binding TypeDisplay}" Header="Loại GD" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding AmountDisplay}" Header="Số tiền" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding RunningBalance, StringFormat=N0}" Header="Số dư sau" />
                                    <DataGridTextColumn Width="*" Binding="{Binding TransactionId}" Header="Mã GD" />
                                    <DataGridTextColumn Width="3*" Binding="{Binding Description}" Header="Diễn giải" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Tài khoản đã mở" Visibility="{Binding CanViewManagementReports, Converter={StaticResource BoolToVisConverter}}">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border
                        Grid.Row="0"
                        Padding="15"
                        Background="#F8F9FA"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Từ ngày:" />
                                <DatePicker x:Name="AccountsOpenedFromDate" MinHeight="32" Padding="10" FontSize="14" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Đến ngày:" />
                                <DatePicker x:Name="AccountsOpenedToDate" MinHeight="32" Padding="10" FontSize="14" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Chi nhánh:" />
                                <ComboBox x:Name="SelectedBranchForAccounts" Height="42" ItemsSource="{Binding AvailableBranches}" Style="{StaticResource StandardComboBoxStyle}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource BranchDisplayConverter}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <Button x:Name="GenerateAccountsOpenedReport" Grid.Column="3" Margin="0,22,10,0" Height="42" Content="Tạo báo cáo" Style="{StaticResource PrimaryButtonStyle}" />

                            <StackPanel Grid.Column="4" Margin="0,12,0,0">
                                <TextBlock Margin="0,0,0,2" FontSize="10" Text="Xuất:" />
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="ExportAccountsToPdf" Width="70" Height="42" Margin="0,0,5,0" Content="PDF" Style="{StaticResource SuccessButtonStyle}" />
                                    <Button x:Name="ExportAccountsToExcel" Width="80" Height="42" Content="Excel" Style="{StaticResource SuccessButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding AccountsOpened}" Style="{StaticResource StandardDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="2*" Binding="{Binding SOTK}" Header="Số TK" />
                            <DataGridTextColumn Width="2*" Binding="{Binding CMND}" Header="CMND" />
                            <DataGridTextColumn Width="2*" Binding="{Binding SODU, StringFormat=N0}" Header="Số dư" />
                            <DataGridTextColumn Width="2*" Binding="{Binding BranchDisplayName}" Header="Chi nhánh" />
                            <DataGridTextColumn Width="2*" Binding="{Binding NGAYMOTK, StringFormat=dd/MM/yyyy}" Header="Ngày mở" />
                            <DataGridTextColumn Width="*" Binding="{Binding StatusDisplay}" Header="Trạng thái" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Khách hàng theo chi nhánh" Visibility="{Binding CanViewManagementReports, Converter={StaticResource BoolToVisConverter}}">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border
                        Grid.Row="0"
                        Padding="15"
                        Background="#F8F9FA"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Chi nhánh:" />
                                <ComboBox x:Name="SelectedBranchForCustomers" Height="42" ItemsSource="{Binding AvailableBranches}" Style="{StaticResource StandardComboBoxStyle}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource BranchDisplayConverter}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <Button x:Name="GenerateCustomersPerBranchReport" Grid.Column="1" Margin="0,22,10,0" Height="42" Content="Tạo báo cáo" Style="{StaticResource PrimaryButtonStyle}" />

                            <StackPanel Grid.Column="2" Margin="0,12,0,0">
                                <TextBlock Margin="0,0,0,2" FontSize="10" Text="Xuất:" />
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="ExportCustomersToPdf" Width="70" Height="42" Margin="0,0,5,0" Content="PDF" Style="{StaticResource SuccessButtonStyle}" />
                                    <Button x:Name="ExportCustomersToExcel" Width="80" Height="42" Content="Excel" Style="{StaticResource SuccessButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding CustomersByBranch}" Style="{StaticResource StandardDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="2*" Binding="{Binding CMND}" Header="CMND" />
                            <DataGridTextColumn Width="2*" Binding="{Binding FullName}" Header="Họ tên" />
                            <DataGridTextColumn Width="3*" Binding="{Binding DiaChi}" Header="Địa chỉ" />
                            <DataGridTextColumn Width="2*" Binding="{Binding SODT}" Header="SĐT" />
                            <DataGridTextColumn Width="2*" Binding="{Binding BranchDisplayName}" Header="Chi nhánh" />
                            <DataGridTextColumn Width="*" Binding="{Binding StatusText}" Header="Trạng thái" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Tổng hợp giao dịch" Visibility="{Binding CanViewManagementReports, Converter={StaticResource BoolToVisConverter}}">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border
                        Grid.Row="0"
                        Padding="15"
                        Background="#F8F9FA"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Từ ngày:" />
                                <DatePicker x:Name="TransactionSummaryFromDate" MinHeight="32" Padding="10" FontSize="14" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Đến ngày:" />
                                <DatePicker x:Name="TransactionSummaryToDate" MinHeight="32" Padding="10" FontSize="14" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="0,0,20,0">
                                <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Chi nhánh:" />
                                <ComboBox x:Name="SelectedBranchForTransactionSummary" Height="42" ItemsSource="{Binding AvailableBranches}" Style="{StaticResource StandardComboBoxStyle}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource BranchDisplayConverter}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <Button x:Name="GenerateTransactionSummaryReport" Grid.Column="3" Margin="0,22,10,0" Height="42" Content="Tạo báo cáo" Style="{StaticResource PrimaryButtonStyle}" />

                            <StackPanel Grid.Column="4" Margin="0,12,0,0">
                                <TextBlock Margin="0,0,0,2" FontSize="10" Text="Xuất:" />
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="ExportTransactionSummaryToPdf" Width="70" Height="42" Margin="0,0,5,0" Content="PDF" Style="{StaticResource SuccessButtonStyle}" />
                                    <Button x:Name="ExportTransactionSummaryToExcel" Width="80" Height="42" Content="Excel" Style="{StaticResource SuccessButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border
                        Grid.Row="2"
                        Padding="15"
                        Background="White"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5"
                        Visibility="{Binding HasTransactionSummary, Converter={StaticResource BoolToVisConverter}}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Thống kê tổng hợp" />
                            <Grid Margin="0,10,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontWeight="Bold" Text="Tổng giao dịch:" />
                                    <TextBlock FontSize="24" Foreground="#0078D7" Text="{Binding TransactionSummary.TotalTransactionCount}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontWeight="Bold" Text="Gửi tiền:" />
                                    <TextBlock FontSize="16" Foreground="#28A745" Text="{Binding TransactionSummary.DepositCount, StringFormat={}{0} giao dịch}" />
                                    <TextBlock FontSize="14" Text="{Binding TransactionSummary.TotalDepositAmount, StringFormat={}{0:N0} VND}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontWeight="Bold" Text="Rút tiền:" />
                                    <TextBlock FontSize="16" Foreground="#DC3545" Text="{Binding TransactionSummary.WithdrawalCount, StringFormat={}{0} giao dịch}" />
                                    <TextBlock FontSize="14" Text="{Binding TransactionSummary.TotalWithdrawalAmount, StringFormat={}{0:N0} VND}" />
                                </StackPanel>
                                <StackPanel Grid.Column="3">
                                    <TextBlock FontWeight="Bold" Text="Chuyển tiền:" />
                                    <TextBlock FontSize="16" Foreground="#17A2B8" Text="{Binding TransactionSummary.TransferCount, StringFormat={}{0} giao dịch}" />
                                    <TextBlock FontSize="14" Text="{Binding TransactionSummary.TotalTransferAmount, StringFormat={}{0:N0} VND}" />
                                </StackPanel>
                            </Grid>

                            <Border Margin="0,0,0,15" Padding="10" Background="#E7F3FF" BorderBrush="#0078D7" BorderThickness="2" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" FontWeight="Bold" FontSize="16" Text="Tổng số tiền giao dịch:" />
                                    <TextBlock Grid.Column="1" FontSize="20" FontWeight="Bold" Foreground="#0078D7" Text="{Binding TransactionSummary.TotalAmount, StringFormat={}{0:N0} VND}" />
                                </Grid>
                            </Border>

                            <Separator Margin="0,0,0,15" />
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Chi tiết giao dịch" />

                            <DataGrid Height="300" ItemsSource="{Binding TransactionSummary.Transactions}" Style="{StaticResource StandardDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Width="*" Binding="{Binding MAGD}" Header="Mã GD" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding SOTK}" Header="Số TK" />
                                    <DataGridTextColumn Width="*" Binding="{Binding LOAIGD}" Header="Loại" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding NGAYGD, StringFormat=dd/MM/yyyy}" Header="Ngày" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding SOTIEN, StringFormat=N0}" Header="Số tiền" />
                                    <DataGridTextColumn Width="2*" Binding="{Binding SOTK_NHAN}" Header="TK đến" />
                                    <DataGridTextColumn Width="*" Binding="{Binding StatusDisplay}" Header="Trạng thái" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
