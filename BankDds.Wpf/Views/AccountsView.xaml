<UserControl
    x:Class="BankDds.Wpf.Views.AccountsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:BankDds.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    Background="White"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:InvertedBoolToVisibilityConverter x:Key="InvertedBoolToVis" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            Style="{StaticResource PageTitleStyle}"
            Text="Account Management" />

        <!--  Error Message  -->
        <TextBlock
            x:Name="ErrorMessage"
            Grid.Row="1"
            Style="{StaticResource ErrorMessageStyle}" />

        <!--  Customer List (Master)  -->
        <GroupBox
            Grid.Row="2"
            Margin="0,0,0,10"
            Header="Customers - Select a customer to view their accounts">
            <DataGrid
                x:Name="Customers"
                SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                Style="{StaticResource StandardDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding CMND}"
                        Header="CMND" />
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding Ho}"
                        Header="Last Name" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding Ten}"
                        Header="First Name" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding DiaChi}"
                        Header="Address" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding SDT}"
                        Header="Phone" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding MaCN}"
                        Header="Branch" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!--  Section Title  -->
        <TextBlock
            Grid.Row="3"
            Style="{StaticResource SectionTitleStyle}"
            Text="Accounts for Selected Customer" />

        <!--  Action Buttons  -->
        <StackPanel
            Grid.Row="4"
            Margin="0,0,0,15"
            Orientation="Horizontal">
            <Button
                x:Name="Add"
                Content="Add Account"
                Style="{StaticResource PrimaryButtonStyle}" />
            <Button
                x:Name="Edit"
                Content="Edit"
                Style="{StaticResource WarningButtonStyle}" />
            <Button
                x:Name="Delete"
                Content="Delete"
                Style="{StaticResource DangerButtonStyle}" />
        </StackPanel>

        <!--  Accounts for Selected Customer (Detail)  -->
        <DataGrid
            x:Name="Accounts"
            Grid.Row="5"
            SelectedItem="{Binding SelectedAccount, Mode=TwoWay}"
            Style="{StaticResource StandardDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding SOTK}"
                    Header="Account Number" />
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding SODU, StringFormat=N0}"
                    Header="Balance (VND)" />
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding MACN}"
                    Header="Branch" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding NGAYMOTK, StringFormat=dd/MM/yyyy}"
                    Header="Open Date" />
            </DataGrid.Columns>
        </DataGrid>

        <!--  Edit Form  -->
        <Border
            Grid.Row="5"
            Style="{StaticResource DetailPanelStyle}"
            Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisConverter}}">
            <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Account Details" />

                    <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Account Number:" />
                    <TextBox
                        Margin="0,0,0,12"
                        Background="#F0F0F0"
                        IsReadOnly="True"
                        Style="{StaticResource StandardTextBoxStyle}"
                        Text="{Binding EditingAccount.SOTK}" />

                    <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Balance:" />
                    <TextBox
                        Margin="0,0,0,12"
                        Style="{StaticResource StandardTextBoxStyle}"
                        Text="{Binding EditingAccount.SODU, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Open Date:" />
                    <DatePicker
                        MinHeight="32"
                        Margin="0,0,0,12"
                        Padding="10"
                        FontSize="14"
                        SelectedDate="{Binding EditingAccount.NGAYMOTK}" />

                    <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Branch:" />
                    <TextBox
                        Margin="0,0,0,20"
                        Background="#F0F0F0"
                        IsReadOnly="True"
                        Style="{StaticResource StandardTextBoxStyle}"
                        Text="{Binding EditingAccount.MACN}" />

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button
                            x:Name="Save"
                            Content="Save"
                            Style="{StaticResource SuccessButtonStyle}" />
                        <Button
                            x:Name="Cancel"
                            Margin="0"
                            Content="Cancel"
                            Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
