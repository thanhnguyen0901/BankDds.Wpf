<UserControl
    x:Class="BankDds.Wpf.Views.AccountsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:BankDds.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    Background="White"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="Tra cứu tài khoản" />
            <TextBlock
                FontSize="13"
                Foreground="#666666"
                Text="{Binding ResultSummary}" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Margin="0,0,0,15"
            Padding="16"
            Background="#F8F9FA"
            BorderBrush="#DEE2E6"
            BorderThickness="1"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Style="{StaticResource StandardLabelStyle}" Text="Số tài khoản hoặc CMND:" />
                    <TextBox
                        x:Name="SearchText"
                        Height="40"
                        Style="{StaticResource StandardTextBoxStyle}" />
                </StackPanel>

                <Button
                    x:Name="Search"
                    Grid.Column="1"
                    Margin="0,22,10,0"
                    Content="Tra cứu"
                    Style="{StaticResource PrimaryButtonStyle}" />

                <Button
                    x:Name="Refresh"
                    Grid.Column="2"
                    Margin="0,22,0,0"
                    Content="Tải lại"
                    Style="{StaticResource SecondaryButtonStyle}" />
            </Grid>
        </Border>

        <TextBlock
            x:Name="ErrorMessage"
            Grid.Row="2"
            Margin="0,0,0,10"
            Style="{StaticResource ErrorMessageStyle}" />

        <DataGrid
            x:Name="Accounts"
            Grid.Row="3"
            SelectedItem="{Binding SelectedAccount, Mode=TwoWay}"
            Style="{StaticResource StandardDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding SOTK}"
                    Header="Số tài khoản" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding CMND}"
                    Header="CMND khách hàng" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding BranchDisplayName}"
                    Header="Chi nhánh mở TK" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding NGAYMOTK, StringFormat=dd/MM/yyyy HH:mm}"
                    Header="Ngày mở" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding SODU, StringFormat=N0}"
                    Header="Số dư (VND)" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding StatusDisplay}"
                    Header="Trạng thái" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid
            Grid.Row="0"
            Grid.RowSpan="4"
            Style="{StaticResource LoadingOverlayStyle}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource LoadingSpinnerStyle}" />
                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Foreground="White"
                    Text="Đang tải..." />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
